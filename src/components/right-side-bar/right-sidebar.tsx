import { component$ } from '@builder.io/qwik';

export default component$(() => {
	// const state = useStore({
	// 	ads: [1],
	// 	images: [1, 2, 3, 4, 5, 6]
	// }, { recursive: true });
	return (
		<rightsidebar style="height: 600px; width: 300px">
		</rightsidebar>
	);
});
